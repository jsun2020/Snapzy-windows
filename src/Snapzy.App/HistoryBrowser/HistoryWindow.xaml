<Window x:Class="Snapzy.App.HistoryBrowser.HistoryWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Width="800" Height="600"
        MinWidth="480" MinHeight="360"
        WindowStartupLocation="CenterScreen"
        Background="#FF1E1E1E">
    <DockPanel>
        <Border DockPanel.Dock="Top" Background="#FF2D2D30" Padding="8,6">
            <DockPanel>
                <ComboBox x:Name="TypeFilter" DockPanel.Dock="Right" Width="110" Margin="8,0,0,0"
                          SelectionChanged="OnFilterChanged" />
                <TextBox x:Name="SearchBox" TextChanged="OnFilterChanged" />
            </DockPanel>
        </Border>
        <Grid>
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <ItemsControl x:Name="Cards" Margin="8">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <WrapPanel />
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                </ItemsControl>
            </ScrollViewer>
            <TextBlock x:Name="EmptyText" Foreground="#80FFFFFF" FontSize="14"
                       HorizontalAlignment="Center" VerticalAlignment="Center" Visibility="Collapsed" />
        </Grid>
    </DockPanel>
</Window>
